pipeline {
    agent any
    stages {
        stage (maven) {
            steps {
                echo "Welcome to Maven section printing maven version"
                sh 'mvn --version'
            }
        }
    }
}
